{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.10"
  },
  "dependencies": {
    "@chainlink/contracts": "^1.2.0"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "npx hardhat ignition deploy ignition/modules/RandomizerOracle.ts --network sepolia --verify"
  }
}
